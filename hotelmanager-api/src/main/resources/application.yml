spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hotel_db
    username: hotel
    password: hotel123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  jackson:
    serialization:
      INDENT_OUTPUT: true

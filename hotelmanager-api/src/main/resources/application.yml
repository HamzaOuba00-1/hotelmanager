server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/hotel_db
    username: hotel
    password: hotel123
    driver-class-name: org.postgresql.Driver
    hikari:
      auto-commit: false

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false

  web:
    resources:
      static-locations: file:uploads/

jwt:
  secret: "MaCléTrèsLongueEtSécurisée_AChanger1234567890!@#"
  expiration: 86400000 # 24h en millisecondes
